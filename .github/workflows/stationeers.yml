name: Stationeers

on:
  push:
    branches:
      - master
    paths:
      - "./**"
      - "!games/**"
      - "games/stationeers/**"

permissions:
  actions: read
  contents: read

jobs:
  build:
    uses: ./.github/workflows/template/push.yml
    secrets: inherit
    with:
      game: stationeers
